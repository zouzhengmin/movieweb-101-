<div class="col-md-12">

  <% if current_user %>
  <div class="movie">
    <%= link_to("编辑", edit_movie_path(@movie), class:"btn btn-primary pull-right") %>
  </div>
  <% end %>

  <h2> <%= @movie.title %></h2>
  <p> <%= @movie.description %></p>

</div>
